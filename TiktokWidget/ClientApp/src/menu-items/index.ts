import apps from "./apps";
import buttonWidget from "./button";
// import dashboard from "./dashboard";
import widget from "./widget";

const menuItems = {
  items: [widget, buttonWidget, apps],
};

export default menuItems;
