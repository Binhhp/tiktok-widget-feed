import { HomeMajor } from "@shopify/polaris-icons";

const apps = {
  id: "apps",
  type: "group",
  title: "Apps",
  label: "Integrations",
  icon: HomeMajor,
  redirect: true,
};

export default apps;
